Exercício proposto:
Microserviço de Pedidos com RabbitMQ
Cenário:
Você vai criar dois microserviços simples:

order-service: Responsável por criar pedidos e enviá-los para uma fila RabbitMQ.

inventory-service: Responsável por consumir os pedidos e processar/verificar estoque.

📦 Estrutura sugerida
1. order-service
Endpoint REST: POST /orders

Modelo: Order { id, productId, quantity, customerEmail }

Ação: Envia o pedido como JSON para uma fila RabbitMQ chamada order.queue

2. inventory-service
Consumidor da fila order.queue

Ao receber o pedido, imprime no console algo como: Verificando estoque do produto X (Qtd: Y) para o cliente cliente@email.com...

Tecnologias:
Spring Boot

Spring AMQP

RabbitMQ

Maven

📌 Requisitos para praticar
Criar os dois serviços com estrutura Maven.

Configurar o RabbitMQ (application.yml ou application.properties).

Criar os modelos (Order.java) e o DTO.

Configurar o RabbitMQ com RabbitTemplate (produtor) e @RabbitListener (consumidor).

Rodar localmente (pode usar RabbitMQ com Docker ou local).

